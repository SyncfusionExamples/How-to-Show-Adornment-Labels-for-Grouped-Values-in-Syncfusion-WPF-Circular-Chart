<Window x:Class="Chart_GroupedValues_DataLabels.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:chart="http://schemas.syncfusion.com/wpf"
        xmlns:local="clr-namespace:Chart_GroupedValues_DataLabels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <local:DataLabelTemplateConverter x:Key="DataLabelConverter"/>
        <local:SegmentBrushConverter x:Key="BackgroundConverter"/>

        <DataTemplate x:Key="customDataLabelTemplate">
            <StackPanel Orientation="Vertical" Background="{Binding Converter={StaticResource BackgroundConverter}}" Margin="5">
                <TextBlock Text="{Binding Converter={StaticResource DataLabelConverter}}"                          
                           Margin="3" Foreground="White">
                </TextBlock>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <chart:SfChart Margin="20">

            <chart:SfChart.Header>
                <TextBlock FontSize="14" Text="Monthly Product Sales Overview" FontWeight="Bold" Margin="10"/>
            </chart:SfChart.Header>

            <chart:PieSeries x:Name="pieSeries"              
                             ItemsSource="{Binding Data}"
                             XBindingPath="Product"
                             YBindingPath="SalesRate"
                             LabelPosition="OutsideExtended"
                             Palette="Custom"
                             GroupMode="Value"
                             GroupTo="1000">

                <chart:PieSeries.ColorModel>
                    <chart:ChartColorModel>
                        <chart:ChartColorModel.CustomBrushes>
                            <SolidColorBrush Color="#FF414141"/>
                            <SolidColorBrush Color="#FF666666"/>
                            <SolidColorBrush Color="#FF666666"/>
                            <SolidColorBrush Color="#FFAAAAAA"/>
                            <SolidColorBrush Color="#FFCCCCCC"/>
                        </chart:ChartColorModel.CustomBrushes>
                    </chart:ChartColorModel>
                </chart:PieSeries.ColorModel>

                <chart:PieSeries.AdornmentsInfo>
                    <chart:ChartAdornmentInfo ShowConnectorLine="True" 
                                              ConnectorHeight="80" 
                                              UseSeriesPalette="True"
                                              ShowLabel="True" 
                                              LabelTemplate="{StaticResource customDataLabelTemplate}"  
                                              SegmentLabelContent="LabelContentPath"                
                                              ShowMarker="False">
                    </chart:ChartAdornmentInfo>
                </chart:PieSeries.AdornmentsInfo>
            </chart:PieSeries>
        </chart:SfChart>
    </Grid>
</Window>
